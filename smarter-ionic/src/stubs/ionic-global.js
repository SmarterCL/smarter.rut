// Stub for ionic-global.js to resolve build issues
export const getIonMode = () => undefined;

// Export as 'b' to match the import in index5.js
export { getIonMode as b };

// Also export other potential named exports that might be expected
export const setupConfig = () => {};
export const getConfig = () => undefined;

export default {};